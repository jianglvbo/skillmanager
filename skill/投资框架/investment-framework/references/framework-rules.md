# 框架边界规则与全局规则

## 框架边界

### 投资心态 vs 投资心得
- 根因是情绪/心理的 → **心态**
- 能靠心态纪律避免的 → **心态**
- 其他有借鉴意义的经验教训 → **心得**
- **没有借鉴意义的直接丢弃**

### 分析框架 vs 交易体系
- 分析方法论、判断框架 → **分析框架**（工具箱）
- 具体买卖条件、执行规则 → **交易体系**（操作手册）
- 两者有交叉时允许交叉引用，各有侧重

### 分析框架内部：方法论 vs 分析档案
- **方法论**：分析方法本身（如"用PE判断企业是否值得买入"）
- **分析档案**：用这个方法论分析具体标的的实例（如"段永平用PE方法分析茅台"）
- 分析档案是方法论的应用案例，通过 wikilink 关联回方法论

## 全局规则

1. 日期格式：原始资源用 `date`（帖子发布日期），框架条目用 `createDate`/`updateDate`（条目创建/更新日期），**均用 `yyyy-MM-dd` 裸写无引号**（Obsidian 才能识别为 Date 类型，可排序 / 做时间线 / Dataview 算间隔）；tags 中的日期同样用 `yyyy-MM-dd`。注：日期字段是唯一例外——即便其他值加双引号包裹，日期也必须裸写，否则退化为 Text 类型
2. 文件命名：中文，不带日期前缀
3. wikilink：完整路径 `[[目录/文件名]]`，不加 emoji
4. 模板文件统一放在 skill 的 `assets/` 目录（非 vault 内），vault 内不设模板目录
5. 框架是活文档，可持续修改迭代
6. 三大归属层（我的/博主/其他）各自包含完整的六大分类
7. 宏观双层存放：顶层 `宏观/` 存通用宏观框架与分析工具（如美林时钟、经济周期指标）；三大归属层下各自的 `宏观/` 子文件夹存该来源对具体宏观事件的分析。跨博主同一事件通过 frontmatter `event` 字段聚合（如 `event: 美联储加息周期`）
8. 行业不按市场分文件夹，用标签区分
9. 个股按市场（港股/A股/美股）分文件夹
10. 博主层按博主名字建子文件夹，档案文件放在该文件夹根目录，按需创建不预建空文件夹
11. 博主的分析框架下有方法论/和分析档案/两个子文件夹；我的和其他的分析框架下不设子文件夹
12. 博主归类仅限博主控制台已登记的博主。**Agent 严禁在粗加工/提炼时自动将新作者「补登」进博主控制台**——若原始资源 `author` 不在控制台，其框架条目一律归「其他」层（未登记投资人层），`author` 字段照实填写但不占用博主层路径、不建博主档案；如需纳入博主追踪，由用户在控制台手动登记后，再由审查/编排迁移至博主层。**任何情况下 Agent 不得就「是否补登」询问用户。**
13. "其他"层只接收投资相关、来自具体投资人的内容；与投资无关的信息直接丢弃
14. 跨层查找靠 frontmatter 标签，不靠文件结构
15. "我的"层由用户自己管理，Agent 不维护
16. 没有借鉴意义的信息直接丢弃，不入库
17. Tags 规则：精简、分级、有用
    - 只加对分类检索有帮助的标签：个股名称、行业、板块、市场
    - 禁止加无分类价值的标签（如 #RIO、#威士忌 等产品名/品牌名）
    - 用 "/" 做多级分级：行业/食品饮料/白酒、行业/食品饮料/葡萄酒、行业/AI与算力/AI应用
    - 个股文件 tags 示例：[百润股份, 行业/食品饮料/白酒, 市场/A股]
    - 行业文件 tags 示例：[行业/农林牧渔/畜牧业, 市场/A股]
    - 宏观文件 tags 示例：[美联储, 货币政策, 2026-07-03]
18. 可读性优先：提炼产物要让读者快速理解、判断是否有用、记住核心观点。两个层面：
    - **修辞层面**：保留原文的比喻、案例、推理链条和生动表达，不做干巴巴的要点罗列。目标是让经验知识有"血肉"、可理解，而非只剩骨架
    - **认知锚点**（帮读者快速建立理解框架）：
      - **一句话核心判断**：正文开头用一句话浓缩全篇精髓，让读者3秒内决定要不要深读
      - **适用条件/边界**：显式标出这个经验在什么条件下成立、什么条件下不成立。没有边界的经验等于鸡汤
      - **反直觉点**：如果原文有"大家以为X，其实是Y"的认知冲突，在开头把张力拎出来
      - **时间敏感性**：标注经验产生的市场环境和时间背景，帮助读者判断是否还适用
19. 忠实原文：可以重组表达、优化结构、补充上下文说明，但绝不捏造、编撰、偏离原文事实。比喻和案例必须来自原文或原文引用的真实来源
20. 脚注体系：三类脚注（关联/数据溯源/时效标注），定义、格式、添加阶段和位置详见 `footnote-taxonomy.md`。提炼阶段可加 `[^data-N]`（数据溯源）和 `[^date-N]`（时效标注）；关联脚注（enhance/supplement/conflict/complement）仅在审查阶段添加。**提炼阶段文件不含 `## 关联` 章节，也不含空脚注占位**（关联脚注在审查阶段随文末 `---` 脚注区一起写入；脚注区以 `---` 分隔线标识，无 `## 脚注` 标题）
21. Frontmatter 引号规则：YAML 值的外层包裹必须根据值内引号情况切换，否则 Obsidian Properties 面板解析异常：
    - 值不含引号 → 双引号包裹：`title: "珀莱雅深度拆解"`
    - 值含双引号 → 单引号包裹：`title: '周期股投资的"债券+期权"思维'`
    - 值含单引号 → 双引号包裹：`title: "错过'时代'是最大的风险"`
    - 值同时含双引号和单引号 → 双引号包裹+转义：`title: "他说\"好\"了"`
    - **禁止**：双引号包裹的值内部出现未转义的双引号（如 `title: "理解"书""`）
22. 提炼输入必须是原始资源且 `status=待提炼`：提炼（investment-refine）的输入必须来自 `工作区/原始资源/` 中 `status=待提炼` 的文档，**禁止**直接在 `工作区/粗制品/` 上提炼。判断以**原始资源为锚点**——若原始资源中不存在该文档的 `status=待提炼` 记录，说明仍在粗制品，编排者须先调用 investment-coarse-processor 完成粗加工（置 `status=待提炼`），再进入提炼。
23. `source` 字段（框架条目）：YAML 列表（数组），支持多个 wiki 链接值，每项须引号包裹 `"[[...]]"`（避免 `[[` 被 YAML 解析为嵌套序列）。**多个来源统一写入 `source` 数组，框架条目正文不再设 `## 来源` 段落**——来源归口到 `source`，与正文 `## 来源` 段落二选一，现统一用 `source` 数组。
24. 繁体转简体：粗制品若为繁体中文，入库的原始资源（含正文）必须转为简体中文；提炼产出的框架条目正文同样使用简体中文。仅做繁简字形转换，**保留原文用词、语气、比喻、案例、推理链条，不改写内容**。粗加工阶段是转换的主责环节（investment-coarse-processor）；提炼阶段（investment-refine）若发现原始资源仍为繁体，作兜底转换。

25. 删除原始资源须连带清理产物与引用痕迹：删除 `工作区/原始资源/` 中某文档时，必须一并清理其所有提炼产物与引用痕迹，保持 vault 无悬空 wikilink / 脚注：
    - 删除该原始资源对应的**所有框架条目**（提炼产物文件，一篇可拆多条故可能多个）；
    - 产物内定义的脚注（文末 `---` 脚注区的 `[^x]` 定义 + 正文中的引用标记 `[^x]`）随文件删除一并清除；
    - vault 内**其他正文**（含博主档案、其他条目）中指向这些产物的 wikilink、关联脚注引用标记（`[^x]: ... [[产物]]` 类）全部清除；
    - **博主层特例**：若被删产物归属层为博主层，且博主档案（`博主/<名>/<名>.md`）中已无其他内容（即仅记录此条/此批产物），则**连博主档案本体一并删除**；若该博主仍保留有其他产物，则仅清理档案内对该条的引用，保留档案骨架。

26. 待回收控制台与 30 天冷静期回收：框架条目若你觉得质量不佳需回收，记入 `工作区/待回收控制台.md`（路径见 investment-framework 路径表 `RECYCLE_CONSOLE`）。
   - **你只填三栏**：`条目`（wikilink）、`添加日期`（yyyy-MM-dd）、`理由`；`状态` 栏留空，由下一次审查计算填入。
   - **撤销 = 删行**：添加日期起 30 天内，你随时可直接删掉该行撤销回收，无任何副作用，条目保持原样。
   - **超 30 天自动回收**：审查时计算 `today - 添加日期 > 30天` 的条目，执行**真删**（文件移出 vault 至系统废纸篓）+ **双向清理**（反查并清除 vault 内所有指向该条目的 wikilink 与关联脚注，含博主档案、其他条目、`source` 字段），保持无悬空链接、不制造孤岛文件。
   - **状态栏由审查填入**：`待回收（剩 N 天）`（未到期）/ `已回收`（已执行，该行作为历史记录保留）。
   - **审查报告须给理由**：每条回收在「待回收处置报告」中写明「超 30 天冷静期 + 用户原始理由 + 清理引用数」，供你复核。
   - **Agent 严格自律**：绝不替用户在控制台预填条目或填 `状态` 栏；绝不缩短 30 天冷静期；超期回收必出理由报告。回收动作严格由「用户填三栏 + 审查超期执行」驱动，Agent 不得单方面发起回收。

27. 层间 frontmatter 字段集边界（防提炼误带字段）：框架条目层与原始资源层使用**不同的 frontmatter 字段集**，提炼时只搬运语义对应的字段，绝不可把原始资源的字段整体照搬进条目：
   - **原始资源层**（粗加工产出，`工作区/原始资源/`）字段集：`title` / `source` / `author` / `date` / `type` / `status` / `tags`（7 字段；`date` = 帖子发布日，`status` = `待提炼`）。**字段顺序建议（防漂移）**：`title → source → author → date → type → status → tags`——粗加工须按此顺序写入。
   - **框架条目层**（提炼产出，落各分类模板）字段集：各分类模板定义者——`title` / `createDate` / `updateDate` / `author` / `tags` / `source` + 分析档案额外 `标的` / `status` + 宏观额外 `event` / `时效状态` / `时间范围`。
   - **硬约束**：框架条目 frontmatter **禁止出现 `date` 字段**（那是原始资源层的发布日字段）；提炼从原始资源生成条目时，仅取 `author` / `source` 等语义对应字段，**绝不把原始资源的 `date` 带入条目**——否则会在条目层制造"流浪 date"，破坏 frontmatter 规范（呼应 #1 日期格式约定）。
   - **字段顺序（canonical）硬约束**：框架条目 frontmatter 字段必须按所属分类模板的 canonical 顺序排列（各 `assets/*.md` 模板即唯一真相源）。标准 6 字段顺序：`title → createDate → updateDate → author → tags → source`；分析档案：`title → 标的 → createDate → updateDate → author → status → tags → source`；宏观事件型：`title → event → 时效状态 → 时间范围 → createDate → updateDate → author → tags → source`。**禁止打乱顺序**（如 `source` 前置、`tags` 置底）。审查（investment-review 结构审查第三步）与 `vault_review.py` 据此检测顺序漂移。
   - **`author` 必填**：每个框架条目必须有 `author`——wikilink 来源从原始资源取博主名；外部 URL 来源填原出处，无则填 `待补`。提炼产出条目若漏 `author` 视为 frontmatter 不完整（与审查自检联动）。
   - 这是当前 vault 内 31 个框架条目误含 `date` 的根因：提炼环节把原始资源的 `date` 一并复制进了条目。

28. 个股文件命名必须带股票代码：文件名格式 `{名称}({代码})`（如 `茅台(600519)`、`苹果(AAPL)`、`Circle(CRCL)`、`融创服务(01516)`、`分众传媒(002027)`），代码不加市场前缀/后缀（即写 `002027` 而非 `SZ002027` 或 `002027.SZ`）。代码确定优先级：
    - **① 文章/原始资源中已明确给出的代码** → 直接采用；
    - **② 文章未给出** → Agent 必须**联网检索**确认正确代码，不得留空或凭记忆臆测；
    - **③ 同时有港股（或美股）与 A 股上市、且文章内容无法区分所属市场** → **默认采用 A 股代码**（如金风科技 A+H 双上市，文章未区分则取 A 股 `002202`）。
    - 文件按规则 #9 归入对应市场子文件夹（A股/港股/美股），代码仅作标题标识、不重复表达市场。
