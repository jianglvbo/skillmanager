# 提炼落库模板（POST /api/refine/record · schema）

> 由 investment-refine SKILL.md 第四步引用。**提炼完成后按本模板规定字段，`MCP 工具 `refine_record`（REST POST /api/refine/record 兼容，连接见 Ai/tools/investment-console-mcp/README.md）` 写入投资看板**，看板完整展示产物 + 决策链路。

---

## 一、落库对象结构（单次提炼 = 一条 record）

```json
{
  "id": "ref-xxx",
  "at": 1786809179530,
  "from": "工作区/原始资源/牛奶行业点评.md",
  "sourceType": "raw",
  "source": "[原文](https://xueqiu.com/.../XXXXXX)",
  "targets": [
    {
      "path": "我的/行业/牛奶行业.md",
      "type": "wiki",
      "layer": "other",
      "category": "industry",
      "tags": ["行业", "周期"],
      "basis": "原文「原奶价格已跌至 2018 年以来低位，去产能进入中后期…」",
      "relation": "new"
    }
  ],
  "reason": "整体拆分决策说明",
  "steps": ["读取原文", "归属层判断", "创建条目", "更新博主档案", "校验"],
  "bloggerUpdated": false,
  "bloggerName": "",
  "verify": { "ok": true, "detail": "verify-format.py 0 问题" },
  "verificationHints": ["该判断可后续建分析档案做结果跟踪"]
}
```

---

## 二、字段规范（必须遵守）

| 字段 | 必填 | 类型 | 说明 |
|:---|:---|:---|:---|
| `from` | ✅ | string | **来源标识**。常规路径＝源材料相对路径（`工作区/原始资源/xxx.md`，真实存在的文件）；**帖子集路径（2026-09-12 起）＝数据库引用 `post_history/{博主} {采集日}`**（如 `post_history/庶人哑士 2026年09月09日`）——帖子集已不落 vault，写批次文件路径会成死链（看板还会生成失效的 Obsidian 打开链接）；不带 `.md` 即按纯文本渲染 |
| `sourceType` | 建议 | string | `raw`=原始资源 / `coarse`=粗制品直提（帖子集 #29 / 直投 #30）；缺省按 from 含「粗制品」推断 |
| `source` | 建议 | string | 原文链接（markdown 格式 `[标题（博主 日期）](url)`） |
| `targets` | ✅ | array | **产物列表**，每项见下 |
| `reason` | 建议 | string | 整体拆分决策说明（为何拆成这些条目） |
| `steps` | 建议 | array | 提炼步骤时间轴（读取→分析→创建→档案→校验） |
| `bloggerUpdated` | 建议 | bool | 是否更新了博主档案（言论追踪） |
| `bloggerName` | 建议 | string | 涉及博主名 |
| `verify` | 建议 | object | 产出校验结果 `{ok, detail}`（verify-format.py） |
| `verificationHints` | 建议 | array | 可验证判断提示（建分析档案建议） |

### `targets[]` 每项

| 字段 | 必填 | 说明 |
|:---|:---|:---|
| `path` | ✅ | 产物相对路径 |
| `type` | ✅ | 英文码：`wiki`=框架条目 / `blogger`=博主言论（DB 落库，画像 md 已退役） / `macro`=宏观 |
| `layer` | 建议 | **英文码**（见下方字典表）：`my`/`blogger`/`other`/`macro`/`workspace` |
| `category` | 建议 | **英文码**（见下方字典表）：`analysis_framework`/`trading_system`/`investment_mentality`/`investment_insight`/`stock`/`industry`/`macro` |
| `tags` | 建议 | 标签（来自标签体系，一级前缀） |
| `basis` | ✅ | **依据**：原文支撑该条目的关键句引用——回答"从哪句话提炼的" |
| `thinking` | ✅ | **思考链路 v2**（2026-08-31）：自由长度对象数组 `[{kind,text,quote?,alt?}]`，按真实推理过程记录（观察/疑问/假设/查证/对比/权衡/排除/决策/结论…）；`quote`=触发该步的原文引用、`alt`=备选方案/否决理由；三个决策点（归属层/拆分/关系）必含。**禁止固定 5 步模板套话**（旧格式字符串数组仍兼容展示） |
| `relation` | 建议 | **英文码**：`new`/`append`/`complement`/`conflict_check`/`other`——判断范围按分层检索链（同作者同类分类 → 全库关键词兜底 → 新建，关键词取产物名拆分+原文实体+标签 3-5 词，检索文件名/标题/标签；见 SKILL.md 第一步第 3 步；与 C7 联动） |

---

## 二B、字典码对照表（避坑 · 2026-08-31 实测）

**`layer`/`category`/`relation`/`type`/`sourceType` 必须传数据库字典英文码，传中文会落库失败（外键约束报错）**。全量对照（源：远端 MySQL `investment_kb` 的 `dict` 单表，`type` 列区分字典域；2026-08-31 schema 整合后由多张 dict_* 合并为单表）：

| 字段 | 字典表 | 合法码（码 → 中文含义） |
|:---|:---|:---|
| `layer` | dict(type=layer) | `my`=我的 / `blogger`=博主 / `other`=其他 / `macro`=宏观 / `workspace`=工作区 / `attachment`=附件 |
| `category` | dict(type=category) | `analysis_framework`=分析框架 / `trading_system`=交易体系 / `investment_mentality`=投资心态 / `investment_insight`=投资心得 / `stock`=个股 / `industry`=行业 / `macro`=宏观 |
| `relation` | dict(type=target_relation) | `new`=新建 / `append`=追加 / `complement`=互补 / `conflict_check`=矛盾预检 / `other`=其他 |
| `type` | dict(type=target_type) | `wiki`=框架条目 / `blogger`=博主言论（DB） / `macro`=宏观条目 |
| `sourceType` | dict(type=source_type) | `raw`=原始资源 / `coarse`=粗制品 |
| 审查 `checks[].status` | dict(type=check_status) | `pass`=通过 / `warn`=警告 / `fail`=失败（`ok`=旧数据遗留，新写入不用；**无 `info`**） |

> **避坑**：落库报 `foreign key constraint fails ... dict_*` 时，用 `SHOW CREATE TABLE {refine_target_sub|review_check_sub}`（`_sub` 子表） + 对应字典表核对码值，不要猜中文。博主言论目标 `category` 可省略（列可空）。

---

## 三、`type` 取值与看板展示

| type | 含义 | 看板展示 |
|:---|:---|:---|
| `wiki` | 框架条目 | 分类色块 + 归属层徽章 + 产物决策卡 |
| `blogger` | 博主言论（落 DB） | **粉色「言论追踪」标记** |
| `macro` | 宏观 | 琥珀色「宏观」标记 |

`sourceType=coarse` 时看板显示「粗制品直提」徽章；`verify.ok` 显示「校验通过」；`bloggerUpdated` 显示「言论追踪」。

---

## 四、决策一致性（质量保障）

- **自由文本路径书写硬约束（2026-09-01 用户确认，前置约束优于回检兜底）**：`reason` / `thinking`(text/quote/alt) / `basis` / `verify.detail` 等自由文本中，`xxx/yyy.md` 完整路径**只允许**指向「写入时已确认存在于 vault 的文件（本次检索命中）」或「本条 `targets[].path` 产物 / `from` 源」；假想、被否决、未创建的条目一律写《名称》（**不带 `.md`、不带路径**）。看板按此语义渲染：`.md` 路径 = 可点击跳 Obsidian，《名称》 = 纯文本。前端存在性校验只是兜底质检，写入方不得依赖它补救
- `basis`（依据原文句）必须**真实来自原文**，禁止事后编撰
- `thinking` 的「决策」步（归属层/拆分/标签判断）必须来自第一步分析的真实判断：归属层铁律（博主控制台登记）、标签体系、同作者一致性预检
- `relation`（关系）与审查 C7 关联备注提案、C3 矛盾预检联动——提炼时标注的互补/矛盾，审查时据此核对
- 落库失败（看板未启动等）不阻断提炼主流程，但汇报中必须提示「看板数据未写入，需补录」

---

## 五、旧数据兼容

- 旧格式 `to[]`（字符串数组）→ 服务端自动归一化为 `targets[]`（type 按路径推断：`博主/`→blogger、`宏观/`→macro、其余→wiki；layer/category 从路径提取）
- 看板读取兼容 `r.to` / `r.targets` 两种结构

## 七、落库前置检查八问（自 SKILL.md 第 1.4 步下沉 · 2026-09-12）

> 由 SKILL.md「第 1.4 步」引用：写入前必过，答错会被服务端拒写，返工成本高。

> **写入前必过**（不要等到自检环节）：每条言论在调用 `blogger_statement` 前，先答完八问（含九项必有字段）。答错会被**服务端拒写**（`_validateSignal` / `form` / 空壳 / sourceUrl 硬门禁），返工成本高。

| # | 落库前置问 | 不合格处理 |
|:--|:---|:---|
| **①** | **这条有方向立场吗？** | 无 → `stance` 与 `signal` **都留空**（研究结论/事实数据/观察笔记属 `view_text` 正文，**不得写入 signal**） |
| **②** | **信号内容具体吗？** | 空泛/类别名/元描述（「行业判断」「中报点评」「投资信念」「复盘」）→ 改写为**具体结论**（事实/数据/理由） |
| **③** | **信号够短吗？** | 超 25 字 → 精简为具体结论（方向由 stance 承载），用 `/` 分隔并列要素；**开头不得再写「看多/看空/中性」** |
| **④** | **正文干净吗？**（2026-09-11 用户确认） | 正文**只写内容本身**：出现 `见 [[…]]`、`（已提炼见 [[…]]）`、`已具象化`、`（信号：复盘）` 等**流程性标记**一律删掉——那不是博主言论，也不是提炼出的含义。已沉淀成框架条目的，条目名写 **`wikiRef`**，卡片自动显示「已具象化：[[条目名]]」 |
| **⑤** | **`form` 填了吗？**（2026-09-11 用户确认） | **`form`（帖子形态：回复/短文/长文/专栏）必填**，与 `contentType` 一样属于必有字段；采集侧摘要行零解析读取，无采集行按同规则判定（`is_column`→专栏 / 原文以「回复@」开头→回复 / ≥300 字→长文 / 其余→短文） |
| **⑥** | **篇幅砍过头了吗？**（2026-09-11 用户确认） | 正文**篇幅不设硬上限**：宁可长一点，也要保住原文的**限定条件、数据、因果与语气**；不得为求短而丢「什么条件下才成立」这类关键信息（信息密度仍要求高，不写空话） |
| **⑧-2** | **新称呼落库了吗？**（2026-09-12 用户要求） | 本批若出现**未登记的个股称呼**（寒王/讯狗/小米 之类）→ 调 `stock_alias(action=add)` 写入 `stocks.aliases`；若该称呼**本身是常用词**（美的/小米/平安）→ 再调 `mark-ambiguous` 落 `dict(type=ambiguous_word)`；顺手 `set-keywords` 补主营词；若本批出现**误判教训/典型例句** → `case-add` 落 `mention_case`（案例进库，别写进 md）。判定规则见 `references/stock-mention-rules.md` |
| **⑧** | **九项必有字段齐了吗？**（2026-09-11 用户确认；**「必有」＝字段存在，不是必须填**） | 内容时间 `viewDate`（判断成立时点，缺省=帖子时间）· 言论时间 `statementDate` · 内容类型 `contentType` · 帖子类型 `form` · 回复 `replyTo`（可空）· 信号 `stance`+`signal`（可空）· 原文链接 `sourceUrl` · 采集时间 `fetchedAt`（抓取日期，缺省今日；**提炼历史批次要传该批次采集日**）· 具象化 `wikiRef`（**填 vault 相对路径**（含 `.md`），如 `博主/X/条目名.md`——卡片点「已具象化」即本地打开；**不要填条目名**；**可空**，已具象化时服务端按 vault 笔记的 source 链接自动反查回填）。**其中 回复 / 信号 / 具象化 三项允许为空**（无回应对象、无方向、未沉淀条目），只有 帖子时间 / 内容类型 / 帖子类型 / 原文链接 必须非空。三个时间别混：帖子时间＝发帖、内容时间＝判断成立、采集时间＝我们抓的日期 |
| **⑦** | **回应字段化了吗？**（2026-09-11 用户确认；**2026-09-12 二次修正＝去包装**） | `//@` 之后**被回应者说的话**写进 **`replyTo`** 字段，正文 `view` **只放博主自己的话**；**禁止**写 `（回应"…"）博主观点` 这种内嵌式（前端已不解析，会当正文原文显示）。**且不许带平台包装**（用户原话：「落的是『回应 //@空仓三十天：回复@metalslime:药神能告诉我科技是看通信etf吗』，我只要『药神能告诉我科技是看通信etf吗』这一段」）→ `//@昵称：` 与嵌套 `回复@昵称:` 一律剥掉，**说话人昵称不进字段**（要查回的是谁，去 `post_history.raw_text`）；内容本身仍一字不改（不提炼/不概括/不截断）。服务端 `_cleanReplyTo()` 会自动剥，但**别指望它兜底**：传带包装的值等于白传一次 |

> **批量数据修复脚本纪律（2026-09-11 教训）**：修正已有言论行（如补 `replyTo`）的脚本**只改目标字段**，不得顺带覆写 `view_text`；从采集文件切段落时终点要从**本贴标题**往后找 `\n> 发布：`（从 URL 往后找会抓到下一篇）；先 dry-run 并打印「旧值→新值」，落库后复检污染指纹（`## `、`发布：`、`回复@`）为 0。完整规则见 framework-rules #43（数据库重构与「帖子只落类型表」见 #44）。

> **批量外部判断的格式风险（2026-09-11 教训）**：把提炼外包给子任务时，务必在任务书里写明「signal 不带方向词前缀」「form 必填」「正文不写 wikilink」「回应写 replyTo 字段、不内嵌正文」「**replyTo 只写对方原话、不带 `//@某人：`/`回复@某人:` 包装**」——否则会成批产出「信号：看多 · 看多…」「回应内容是雪球语法串」这类需要事后返工的数据。


**合法信号形态**（三个范例）：
```
看多 · 白酒跑赢科技 / 未来半年到一年
中性 · 中报：利润同比降但好于预期
看空 · 情怀流量无效（转化1.49%）
```

**服务端门禁**（写不进去的 8 类：另加「缺帖子时间」「回复类缺 replyTo」「预测缺 stance」「dict 外的 form」）：无方向却有 signal / signal 自带方向词前缀 / 占位符 `—` / 元描述 / 话题标签 / 超 25 字；另加 `form` 必填、正文与信号不能同时为空、`sourceUrl` 必填。


## 八、框架条目写入硬约束（自 SKILL.md 第二步下沉 · 2026-09-12）

> 权威源仍是 `framework-rules.md` 对应编号；此节为执行摘要，冲突以 framework-rules 为准。

**写入硬约束（创建/修改任何文件前必须满足，违反即重写）**：完整规则以 `framework-rules.md` 对应编号为权威源，此处为执行摘要——若与 framework-rules 冲突，以 framework-rules 为准：

- **wikilink 完整路径**（#3）：所有 wikilink（正文、source 字段、博主档案言论追踪的具象化指针）写完整路径 `[[博主/名/分类/文件名]]`，**禁止仅写 basename**。唯一例外：博主档案内指向自身文件夹下条目时可省略 `博主/{名}/` 前缀
- **个股文件名带代码**（#28）：`{名称}({代码})`，代码缺失时联网检索确认，不得留空
- **禁止 `## 来源` 段落**（#23）：来源一律写入 frontmatter `source` 数组，正文不设该 section
- **`source` 填真实原文链接**（#23/#29）：`source` 数组填原文链接（markdown 格式 `[标题（博主 日期）](https://xueqiu.com/.../XXXXXX)`，取自批次 `[原文]` 链接），**禁止填采集批次文件名**（批次提炼后即删，会成悬空引用）；综合多篇帖则列多个链接
- **模板 section 全量输出**：按所属分类模板的 section 结构全量输出（标题 + 内容）。某 section 确实无内容时写一句最小说明（如"暂无验证记录"），**不得省略 section 标题**
- **frontmatter 字段顺序 canonical**（#27）：按模板定义的字段顺序写入，禁止打乱
- **`star` 缺省 false**（#27）：内容型条目（方法论/分析档案/交易体系/心态/心得/行业/宏观/个股）frontmatter 一律含 `star: false`（位于 author 后），**提炼阶段不推断不打星**，由用户手动改为 true
- **`delete` 常驻空值**（#26/#27）：内容型条目 frontmatter 一律含 `delete: `（空值，位于 star 后），**提炼阶段不填值**——回收标记完全由用户手动填日期，Agent 绝不代填
- **日期裸写**（#1）：`createDate`/`updateDate` 裸写 `yyyy-MM-dd`，无引号
- **原文链接格式**（#30/#35）：表格内链接一律用 `[原文](URL)` 格式，**禁止贴裸 URL**
- **产出物去 emoji**（2026-08-09 用户确认边界）：wiki 产物与落库文本（框架条目、`view_text`/`signal_text`/`strengths`/`limitations` 等 DB 字段）**禁止任何 emoji 表情**——标题、正文、frontmatter 均不得含 emoji 表情字符；`→` 流程箭头、表格 `---` 分隔线等非表情符号不受限。**该边界只影响提炼环节**：采集（post-fetch）与粗加工（coarse-processor）必须保持原文（含原文 emoji），不得清洗


## 六、博主言论分流决策矩阵（自 SKILL.md 第 1.5 步下沉）

雪球博主帖子**每帖只落一条言论**（2026-09-11 用户拍板），**按优先级顺序判定类型、命中即止**。采集只给发帖时间与形态，内容分类与观点时间在本步判定。

**单帖归类规则（优先级判定，2026-09-11 固化，硬约束）**：

| 优先级 | content_type | 命中条件（自上而下问，首个「是」即归此类，**不再往下判**） |
|:---:|:---|:---|
| **1** | `trade` 买卖记录 | **须有「当期性」**（当下/近期的可跟踪操作）：① 当下或近期的买卖动作（买入/卖出/加仓/减仓/清仓/换仓），或 ② **当前**仓位/持仓/杠杆状态（"持仓5%""无融资仓位""仓位降到X成"），或 ③ 当下交易要素（价格/市值）。<br>**排除历史操作回顾**——讲述多年前的买卖（如「1.1 买五粮认购赚十几个点跑了」）**不是买卖记录**，是复盘/心得 → 归 `insight` |
| **2** | `predict` 预测记录 | 可验证的**未来判断**：方向 + 未来指向（时间窗或事件条件）+ 可判对错，三要素齐备 |
| **3** | `research` 研究 | 含**分析过程或数据支撑**（财报/公告/产业链/估值测算/调研/对比），非纯结论 |
| **4** | `insight` 心得总结 | **方法论/纪律/原则/复盘**——可复用经验，含**历史操作回顾 + 认知反思**（如「1.1 买五粮认购赚十几个点跑了，后面一年涨到 55 块；认知不到位买了也握不住」→ 归 `insight`，**不判方向**：讲的是过去，无当下立场） |
| **5** | `view` 观点 | 当下判断/认知（无未来指向、无数据支撑） |
| **6** | `chat` 闲聊 | 以上皆非：社交互动、情绪表达、能刻画「擅长与局限」的自述 |

- **一帖一条**：一帖含多主题/多标的时，**取优先级最高者落库**，其对象即 `target`，其余内容不单独成条
- **自上而下首次命中即停**：例「既含买卖动作又含观点」→ 归 `trade`（不再往下判 view）
- **判不准不得推给 `chat`**：`chat` 仅限上表第 6 行两种情形；判不准须回到第 1 行重判
- `stance` 仅在落 `trade`/`predict` 时必填

**信息密度门槛（2026-09-11 用户确认，硬约束）**：以下内容**不落库、不建条目**——提炼库的价值在「博主怎么看」，不在「发生了什么」：

| 丢弃类型 | 例 | 判据 |
|:---|:---|:---|
| **通知/预告** | 「晚上8点，不见不散」（直播预告） | 无实质内容，纯通告 |
| ~~纯资讯转述~~ | ***不丢弃*** | **2026-09-11 修正**：转述本身可表达态度（选择转载即立场）——须尝试提炼隐含观点（关注/认可/看好某标的），归 `research` 或 `view`（可带信号）；**仅当转述内容完全中性且无任何指向时**才丢弃 |
| **零信息量** | 纯社交寒暄、无观点的情绪宣泄 | 去掉后无剩余信息 |

> **判定法**：**去掉后可提炼的信息 = 0** → 丢弃。丢弃不写库、不建条目，仅在提炼汇报中列出数量与理由；**遇到「六类全不命中但又不像上述三类」的内容，禁止静默丢弃，须单列「⚠ 待确认分类」交用户裁决**。

### 用户删过的类型（低质帖判据清单 · 2026-09-12 用户要求，**只增不改，由审查维护**）

用户原话：「我的复核可能会有删除的建议，但是我会附上删除的理由，这个规则你也记住，审核到删除的情况也不要意外，因为**有的帖子质量不够，但是你提炼了**，这种我就会在复核意见写上删除，然后你审核后要记得删除，并且内化规则，**减少这种帖子提炼成言论的情况**」。

→ 用户在「待复核」页给出 `verdict=delete` 的意见（**必附理由**）时，审查必须：① 真的删掉那条言论；② 把**理由**作为判据追加到下表。**提炼前必须逐条比对下表**——命中即不落库（理由里常带可泛化的判据词）。

| # | 用户删除理由（原话摘要） | 泛化判据（提炼时据此不落库） | 日期 · 言论 |
|:--|:---|:---|:---|
| — | *（暂无：目前还没有用户删过的案例）* | — | — |

> **维护方式**：审查处理 `pending_review` 的 `verdict=delete` 项时追加一行（理由原话 + 泛化判据 + 日期/言论 id），并在 `internalized` 里写「已删除言论#id；规则：本表第 N 行」。**这张表是提炼侧"该不该落库"的权威清单**，比抽象原则更能拦住复发。

**雪球组合不是主题（2026-09-12 用户指出 `miniAAA` 被误建为个股）**：帖子里形如 `$组合名(ZH123456)$` 的是**雪球组合**（往往是别人的组合），**不入个股/行业/市场三大主题**，也不建 `console_ensure_subject`。处理方式：组合名连同代号原样留在 `target` 文本里（如 `miniAAA(ZH3207194)`），正文照录；组合的收益/调仓本身不构成对该组合所持个股的判断。**同类**：纯小写拉丁短名（如 `cww`）是博主的代称/未识别代号 → 先用原文线索还原成真名，还原不了就只留 `target` 文本、不建主题（不要拿代号当主题名）。服务端已内置门禁拦截这两类。

**实体与提及判定（2026-09-12 新结构）**：言论落库时按 `investment-refine/references/stock-mention-rules.md` 判定文中出现的词是不是个股（关键词邻接 + 二元分类特征），并写 `statement_stock_rel`；行业/市场命中 `industries.name` / `markets.name` 才建对应 rel。**言论一定挂博主**；**个股一定挂行业**（`stock_industry_rel`，可多行业，建股时用 `industryName` 传入）。**组合（`$名称(ZH123456)$`）与未识别代号不入实体**，只留 `target` 文本。

**优先级（2026-09-10 更新）**：**P1 必查必录** = `trade 买卖记录` / `research 研究` / **`predict 预测记录`**（2026-09-10 用户拍板由 P2 提升）→ P2 = `view 观点` / `insight 心得总结` → P3 `chat 闲聊`（高门槛）。

> **`predict` 提为 P1 的理由**：预测是**可验证判断**，漏录即永久丢失验证样本（无法回补准确率）；且看板预测控制台的验证闭环依赖言论库侧可查。

| content_type | 涉及个股/行业/市场 | 去向 | 另落 wiki？ |
|---|---|---|---|
| `trade` | 必是 | `blogger_trade` → **买卖帖言论行 `statement_trade`**（一帖一表；原独立表 `blogger_trades` 已并入并退役为 `blogger_trades_del`；画像 md 已退役不回写）。**方向 `stance` 必填**（看空/看多/中性），有价必标 `price`（开盘竞价位），`note` → `trade_note` 只记操作理由。**结构化字段**：`op`/`price`/`marketCap`/`tradeDate`/`targetAlias`（原文代称）/`tradeNote` | 否 |
| `research` | 是 | `blogger_statement` → `statements` 表。**结构化字段**：`dataRefs`（数据来源）/`wikiRef`（已具象化条目） | 仅当沉淀出可复用框架（言论库记事实，wiki 记方法，**不重复记录**） |

> **`wikiRef` 六表统一（2026-09-11；2026-09-12 改存路径）**：任何 `contentType` 都可填 `wikiRef`——**值是 vault 相对路径**（含 `.md`，如 `博主/Benjm_修/技术革命的领头羊悖论（1）.md`），**不是条目名**；卡片显示「已具象化：<文件名>」并可点击**在 Obsidian 本地打开**（前端拼 `obsidian://open`，路径改了也不失效）。留空时服务端按原帖 URL 反查自动回填。此前仅 research/insight 两张分表有此列，回采历史指针行时发现 view/chat/trade/predict 的链接会丢，故四表补列并重建 UNION 视图。
| `research` | 否 | — | 能成框架 → wiki（我的/其他/宏观）；不能 → **舍弃** |
| `predict` | 必是 | `blogger_statement` → `statements`(predict) + 预测控制台。**`stance` 必填**，`view_date` 取博主下判断的时点。**结构化字段（2026-09-10 新增）**：`refPrice`/`targetPrice`/`targetDate`/`datePrecision`/`statusCode`/`verifyDate`/`verifyResult` —— 预测验证闭环在言论表内可直接查询，免 JOIN（预测与验证留痕：`statement_predict` + 子表 `statement_verify_sub`） | 否 |
| `view` | 是 | `blogger_statement` → 言论库（看板言论追踪）。**不填 `stance`**（2026-09-11 用户确认：方向只在买卖/预测需要） | 否 |
| `view` | 否 | — | 有价值 → wiki；无价值 → 舍弃 |
| `insight` | 是 | `blogger_statement` → 言论库（看板言论追踪）。**结构化字段**：`wikiRef` | **方法论的价值＝有没有产物**：可迁移的原则必须具象化成框架条目并回填 `wikiRef`（卡片显示「已具象化：<文件>」）；只描述经历的留在正文，不额外打标签 |

> **`predict` 的结构化信息去向（2026-09-11 收敛：预测即言论行）**：预测类言论落库后，**参考价/目标价/目标时间/状态/验证结果就写在这一行 `statement_predict` 本体**（`ref_price`/`target_price`/`target_date`/`date_precision`/`status_code`/`verify_date`/`verify_result`）——独立预测表 `predictions` 已退役为 `predictions_del`，`prediction_stmt_rel` 亦随之退役（预测与言论本来就是同一行，无需关联表）。调用方照常传 `refPrice/targetPrice/targetDate/datePrecision/verify*`；`subjectId` 仍是主题入参（同时写入维度关联表）。
| `insight` | 否 | — | 有价值 → wiki；否则舍弃 |
| `chat` | — | 仅当能刻画「擅长与局限 / 投资心态」→ `blogger_statement`；否则**舍弃** | 否 |

**全类型共同字段（2026-09-10 新增）**：`form`（帖子形态：回复/短文/长文/专栏）——取自采集侧摘要行，**零解析读取**，用于分流先验与质检（专栏/长文判 `research` 概率高；回复必先做 `//@` 切分）。**`form` 与 `contentType` 同属必填**（2026-09-11 用户确认）：每行都必须能回答「这条是什么类型的内容 / 什么形态的帖子」；采集侧有摘要行时零解析读取，回补历史行时按采集侧同一规则判定（`is_column`→专栏；原文以「回复@」开头→回复；正文≥300 字→长文；其余→短文）。

**九项必有字段（2026-09-11 用户确认）**——**注意语义：（a）「必有」＝ schema 里必须有这个字段（列），不是必须填内容；（b）回复 / 信号 / 具象化三项均可为空**（没有回应对象、没有方向立场、没沉淀成条目时就是空的），服务端对这三项**不做拒写、只给非阻断提示**；（c）真正**必须非空**的只有：帖子时间、内容类型、帖子类型、原文链接（内容时间与采集时间缺省即有）：

| # | 字段 | 含义 | 缺省与加严 |
|:--|:--|:--|:--|
| ① | `viewDate` 内容时间 | **该判断成立的时点** | 缺省＝帖子时间；帖中若自述「我在 8 月 5 号就看好X」→ 填 8 月 5 号，并给 `viewDateSource=explicit` + `viewDateBasis=原文句`。**这是与帖子时间区分的关键字段**（示例：8/5 看好黄酒、9/10 才发帖 → 内容时间 8/5，帖子时间 9/10） |
| ② | `statementDate` 帖子时间 | 帖子上屏日期 | **必填**（服务端拒写） |
| ③ | `contentType` 内容类型 | 预测/买卖/研究/心得/观点/闲聊 | 必有（六分法） |
| ④ | `form` 帖子类型 | 回复/短文/长文/专栏 | **必填**（服务端拒写） |
| ⑤ | `replyTo` 回复 | 被回应的对方**说的话**（原话本身，不带 `//@某人：` 包装） | **可空**（无回应对象/采集原文无 `//@` 段则留空）；有则照抄原话并**剥掉** `//@某人：` 与嵌套 `回复@某人:`（2026-09-12 规则 v3）——例：原文 `//@空仓三十天：回复@metalslime:药神能告诉我科技是看通信etf吗` → `replyTo` 只写「药神能告诉我科技是看通信etf吗」。**形态先看留档**：`form` 必须以 `post_history.form` 为准，标错成「回复」会让卡片去找不存在的被回应者原话（2026-09-12 修了 43 条这类误标） |
| ⑥ | `stance`+`signal` 信号 | 方向 + 具体结论 | **可空**（无方向立场则留空，页面不显示信号行）；`predict` 通常带方向，缺方向时服务端给提示建议复核归类 |
| ⑦ | `sourceUrl` 原文链接 | 原帖 URL | **必填**（规则 #35） |
| ⑨ | `wikiRef` 具象化 | **本条被提炼进了哪个框架条目文件**（笔记名） | **可空**（未沉淀成条目就留空）；已具象化则服务端自动反查回填。写库时服务端会自动反查 vault（笔记 frontmatter `source` 里的原帖 URL）并回填，人工漏填也不会丢——但提炼时若刚创建/追加了条目，应主动填上；对应条目已删除时要清空 |
| ⑧ | `fetchedAt` 采集时间 | **抓取该帖的日期**（≠ 帖子时间、≠ 入库时间） | 缺省＝今日；**提炼历史批次时传该批次的采集日期**（取自帖子集 frontmatter `recorded` 或文件名日期） |

> **三个时间必须分清**：`statementDate` 帖子时间（9/10 发帖）→ `viewDate` 内容时间（8/5 就看好）→ `fetchedAt` 采集时间（9/11 抓的）；`created_at` 只是入库时刻，不替代采集时间。

**字段化铁律（2026-09-11 用户确认）**：卡片上的每个要素都必须落在**独立的、有类型的字段**里，**禁止把多个要素拼进一个文本字段再靠正则拆**：

| 卡片要素 | 字段 | 类型 | 说明 |
|:---|:---|:---|:---|
| 类型 | `contentType` | enum（六分法） | 观点/研究/预测/心得/闲聊/买卖记录 |
| 形态 | `form` | enum（回复/短文/长文/专栏） | 采集侧判定 |
| 标的 | `target` | text | 名称(代码) |
| 正文 | `view` | text | **只放博主自己的话** |
| **回应** | **`replyTo`** | text（**不限长**，2026-09-12 由 varchar(500) 改 TEXT） | **被回应者自己说的话**——只存原话本身，**原样照抄**且**不带平台包装**（`//@某人：`、`回复@某人:` 一律剥掉）；**不得再内嵌进 `view`** |
| 信号 | `stance` + `signal` | enum + text | 方向 + 具体结论 |
| 原文 | `sourceUrl` | url | — |
| 已具象化 | `wikiRef` | text | 框架条目名 |
| 待读 | `is_read` | tinyint | **系统列，不由提炼填**（默认 1=已读；0=待读，用户在「言论追踪」划过即置已读，见 framework-rules #51） |

> **`//@` 切分落位（配套规则 v3，2026-09-12 用户报错后拍板）**：第一个 `//@` **之前** = 博主本人原话 → `view`；**之后** = 被回应者的话 → `replyTo`。v2「原样照抄全部内容」保留，但**新增「去包装」**：
> 1. **禁止提炼/概括/改写**——不得把「财主，当下的腾讯，您怎么看」写成「「当下的腾讯怎么看」」，不得把一段质疑写成「对其看好泰它西普的质疑」；
> 2. **禁止截断、禁止省略号**——旧写法末尾加 `…` 截到 50/500 字属违规（列已改 TEXT，实测最长 1647 字）；
> 3. **禁止带包装（v3 核心，用户原话）**：库里落成 `//@空仓三十天：回复@metalslime:药神能告诉我科技是看通信etf吗` 是**错的**，只要 `药神能告诉我科技是看通信etf吗` 这一段 → **`//@昵称：` 引用块署名与嵌套的 `回复@昵称:` 标注都要剥掉**（变体 `//[@昵称](url):`、`回复[@昵称](url):` 同样剥）；**说话人昵称不进 `replyTo`**——引用结构完整留在 `post_history.raw_text` 留档，要查「回的是谁」去留档查。**v2 的「保留说话人」条款已作废**；
> 4. **没有就留空**：采集原文里没有 `//@` 段时留空（**不许用概括顶上**）——看板会显示「被回应者内容未采集（点编辑可补录）」，这才是正确信号；
> 5. **自回复也算**：`//@` 引的是博主本人（uid 相同）时为自回复链，照抄他本人旧话即可（同样不带包装）。
> **落库自动兜底**：服务端 `_cleanReplyTo()`（`server.js`）在写入口剥包装——传了带包装的值不会报错，但会被清洗（等于白传）；前端 `stripReplyWrapper()` 渲染侧再兜一层。
> **批量回填工具**：`investment-console/scripts/backfill-reply-to.js`（从 `post_history.raw_text` 抽 `//@` 段，空值写入 / 截断升级 / 概括改写，改写明细存 `backups/reply_to_verbatim_audit.tsv`）；**存量去包装**：`investment-console/scripts/strip-reply-wrappers.js`（2026-09-12 首次执行改写 304 行，备份 `backups/reply_wrapper_strip_20260912.json`，幂等）。
> **已废弃写法**：`（回应"…"）博主观点` 内嵌式（2026-09-11 前使用）——前端已不再解析，历史 224 行已迁移进 `replyTo`。
> **前端渲染**：`replyBodyHtml(view, replyTo)` 优先读字段，仅对历史数据回落解析内嵌写法。

**正文（`view_text`）篇幅与纯净性（2026-09-11 用户确认）**：

| 规则 | 说明 |
|:---|:---|
| **篇幅放开，优先保原意** | 正文是**由博主言论提炼**来的，不设硬上限——宁可长一点，也要**保住原文的限定条件、数据、因果与语气**；不得为了短而砍掉「什么条件下才成立」这类关键信息。信息密度仍要求高（不写空话、不重复口号） |
| **不得混入加工痕迹** | 正文**只写内容本身**。禁止出现 `见 [[…]]`、`（已提炼见 [[…]]）`、`已具象化`、`（信号：复盘）` 等**流程性标记**——那些是提炼动作的副产物，不属于博主言论，也不是提炼出的含义 |
| **具象化一律走 `wikiRef`** | 该条已沉淀成框架条目时，条目名写进 `wikiRef` 字段，卡片自动渲染「已具象化：[[条目名]]」；**不要**把 wikilink 写进正文（用户 2026-09-11 明确：正文里出现 `[[AI医生与医疗AI投资逻辑]]` 这种是错的） |

**`predict` 判定三要素（2026-09-04 用户定义）**：① 明确方向（看多/看空/中性）② 未来指向（时间窗："几年后/几个月后/年内"，或事件条件："美国加息/降息""地缘冲突缓和"等）③ 可判对错（含目标位/幅度/点位最佳）。缺任一要素即回落 `view`（当下判断）或 `insight`（复盘心得）；历史复盘叙述**不得**算预测。**易错例**：`白酒处于底部（公募持仓全面退出为信号）` → `view` + `stance=bullish`（当下判断，无未来时间窗/无可验证目标位），**不得归 `predict`**（同 framework-rules #30「易错判据（观点 vs 预测记录）」）。

**信号（`stance`）规范（2026-09-11 用户确认扩展）**：**所有 `content_type` 均可携带信号**（研究/心得/观点/闲聊都可表达方向），**无信号则不填**（页面不显示该行）。

**信号取值（6 种方向 + 中性）**：

| 码值 | 含义 | 适用 |
|:---|:---|:---|
| `bullish` | 看多 | 不限时间的方向 |
| `bearish` | 看空 | 不限时间的方向 |
| `short_bullish` | 短期看多 | 明确短期（数日~数周） |
| `short_bearish` | 短期看空 | 明确短期 |
| `long_bullish` | 长期看多 | 明确长期（数季~数年） |
| `long_bearish` | 长期看空 | 明确长期 |
| `neutral` | 中性 | **正式取值**：明确表达「不好不坏/多空交织/结果未定」的判断（**不是「无信号」**） |

> 时间维度**只在原文明确时**用 short/long 变体；泛泛看好用 `bullish`，不得臆测时间跨度。

**信号依附于方向（2026-09-11 用户确认，硬约束）**：`signal_text` 是 `stance` 的**内容补充**，**必须依附方向存在**：

| 情形 | `stance` | `signal_text` | 页面 |
|:---|:---|:---|:---|
| 有方向判断 | 填（6 种+中性） | 填具体内容 | `信号：方向 · 内容` |
| **无方向**（研究结论/事实记录/观察笔记） | **留空** | **必须留空** | **不渲染** |

- **研究要点、事实数据、观察记录属于 `view_text`（正文），不得写入 `signal_text`**
- 反例：`research` 类「大模型转全栈竞争 / Gemini降价腰斩」——这是**研究结论**（无方向），不是信号 → 该条 `stance`/`signal_text` 均应留空
- 判据：**这条记录有方向立场吗？没有 → 两个字段都不填**

**信号书写规范（2026-09-11 用户确认，硬约束）**：信号由两个字段合成，页面显示为 **`信号：{方向} · {具体判断}`**：

| 字段 | 内容 | 例 |
|:---|:---|:---|
| `stance` | 方向码值（6 种 + 中性） | `bullish` → 显示「看多」 |
| `signal_text` | **简短的具体判断**（目标 ≤20 字 / 上限 25 字） | `白酒跑赢科技 / 未来半年到一年` |

| 规则 | 说明 |
|:---|:---|
| **简短明了** | 一句话说清，**目标 ≤20 字、硬上限 25 字**；用 ` / ` 分隔并列要素（标的、判断、时间窗）。达标以「信息密度」为准：宁可 21-25 字的完整结论，不要 15 字的空泛表述 |
| **不得只有方向** | 信号内容必须给出**具体指向**（标的/判断/时间窗），**不能只写"看多/看空"**——除非原文确实只能判断出方向（如"我看好，但说不出具体"） |
| **不得自带方向词前缀（2026-09-11 用户报错新增）** | `signal_text` **开头不得再出现「看多/看空/看涨/看跌/短期看多/短期看空/长期看多/长期看空/中性」**：方向由 `stance` 承载，页面渲染为 `信号：{方向} · {内容}`，再写方向会出现 `信号：看多 · 看多 …` 的重复。<br>❌ `stance=bullish` + `signal_text="看多 · 医药刚回本，高位加仓难"` → 渲染「看多 · 看多 · 医药刚回本…」<br>✅ `stance=bullish` + `signal_text="医药刚回本，高位加仓难"`<br>❌ `signal_text="看多铀 · 稀缺性支撑"` ✅ `signal_text="铀 · 稀缺性支撑"`（标的名可保留，方向词必须去掉）<br>**服务端已加硬门禁**（`_validateSignal` 第 ⑤ 条，写入即拒），前端 `stripDirPrefix` 兜底剥离历史数据 |
| **一帖多信号（2026-09-11 用户确认）** | 一条帖子常含多个信号，判定时**必须逐个识别，不得只提炼一个**：<br>· **同向多结论** → 仍落一条，`signal_text` 用 ` / ` 分隔（如 `创新药跌了回补 / 医药刚回本`）<br>· **异向（一多一空）或多标的** → 按下方判据 ⑥「多对象必拆」**拆成多条**（每条一个 target + 一个 stance）<br>· **实例**：metalslime「减仓也没卖到最高；创新药这些是跌了我会回补的方向，AI4S 啥的我肯定不会回补，卖了就卖了」= 两个异向信号 → 拆两条：`trade · AI4S · 看空 · 已卖出不再回补` + `view · 创新药 · 看多 · 跌了会回补` |
| **时间窗写进内容** | 有明确时间跨度时写在这里（如 `/ 未来半年到一年`），不要臆造 |
| **中性也是信号** | `neutral` 与「留空」是两件事：<br>· `neutral` = 博主**明确表达了中性判断**（多空交织/结果未定）→ 填 `stance=neutral` + 具体内容<br>· **留空** = 博主**未表达方向** → 信号行不渲染 |
| **示例（用户认可的写法）** | `信号：看多 · 白酒跑赢科技 / 未来半年到一年`<br>`信号：看多 · 铀：稀缺性支撑，估值偏高`<br>`信号：中性 · 中报：利润同比降但好于预期`<br>`信号：短期看空 · 创业板破位后反弹结束` |

> **判据**：删掉方向后，剩下的内容还能不能让人知道"博主在说什么"？不能 → 信号内容不合格，须补具体指向。

**禁止笼统标签（2026-09-11 用户确认，硬约束）**：信号内容**不得只写话题名/动作名**——那只是「在说什么话题」，不是「判断是什么」。必须写出**具体事实或结论**：

| 反例（不合格） | 问题 | 改写（合格） |
|:---|:---|:---|
| `中性 · 中报点评` | 看不出中报怎么样 | `中性 · 中报：利润同比降但好于预期` |
| `看多 · 业绩分析` | 看不出结论 | `看多 · 净利同比+30% 超预期` |
| `看空 · 行业跟踪` | 看不出依据 | `看空 · 排产环比下滑` |
| `看多 · 复盘` | 零信息 | `看多 · 上一轮同形态后涨 3 倍` |
| `看多 · 基本面判断` | **元描述**——只说了「我做了个判断」 | `看多 · 毛利率环比改善` |
| `看多 · 投资信念` | **概念名**——信念内容是什么？ | `看多 · 相信管理层执行力` |

- **禁止词**（单独出现即不合格）：点评 / 分析 / 跟踪 / 复盘 / 梳理 / 解读 / 汇总 / 思考 / 记录 / 更新 / 分享
- **禁止元描述/类别名（2026-09-11 补充）**：『X 判断』/『X 观点』/『X 信念』/『X 逻辑』/『X 框架』/『X 体系』这类**只描述「哪一类」而不说「是什么」**的表述一律不合格。常见不合格组合：基本面判断、估值判断、投资信念、行业观点、成长逻辑、周期框架。**必须替换为该判断的具体内容**（事实、数据、结论、理由）
- **判据（一句话）**：信号必须回答『**是什么**』（具体事实/结论），而不是『**哪一类**』（话题名/概念名）。单独读信号内容若只能知道「他在谈哪个话题」、不知道「他的结论是什么」→ 不合格
- 注意与「不得只有方向」的区别：那是**缺内容**，这是**内容空泛**——两者都要补具体指向

判据如下：

| 判据 | 规则 | 示例 / 易错 |
|:---|:---|:---|
| **① 主语锚定** | stance 判的是**落库 `target`** 的立场，**不是整段语气**；多标的时 target 取「优先级最高内容」所指对象 | 一段同时说「SOX 估值低」与「白酒将跌」，落库内容为白酒判断 → target=白酒 |
| **② 方向词表** | 看多：看好/低估/买入/加仓/机会/底部/弹性；<br>看空：远离/规避/下跌/风险/减持压力/业绩下行/负α/杀估值/出货；<br>中性：观察/不确定/两可/条件性 | 「**远离**…板块」→ bearish |
| **③ 论据不反转结论** | 为论证看空而引用的「估值低/便宜/到底了」是**论据**，**不改变看空结论** | 「白酒估值到底了，**但**下跌动力来自业绩下行负α」→ **bearish**（非 neutral） |
| **④ 隐晦表达要译** | 类比、俗语、反讽须还原方向 | 「跟白酒**一桌儿**」＝同跌 → bearish |
| **⑤ 条件句按性质归** | 「若 X 则 Y」且 X/Y 可判 → `predict`（事件条件）；仅当下倾向 → `view` | 「真流动性抽离时一波下跌也不是不可能」→ 条件性风险（可 `view`+bearish，或按事件条件归 predict） |
| **⑥ 单帖一条** | **一帖只落一条言论**（不拆条）；多主题时取**优先级最高**者，其对象即 target | 药神例：整段归 `predict`（见下方实例） |
| **⑦ 无信号不填** | 信号是**可选**的：原文未表达方向 → **留空**（页面不渲染信号行），**不得强行编造方向**。<br>但若博主**明确说了中性判断**（如「中报利润同比降但好于预期」「多空因素交织」）→ 应填 `stance=neutral` + 具体内容 | `neutral`（明确中性）≠ 留空（未表达方向）——前者要渲染，后者不渲染 |

**判据实例（2026-09-11 用户提供，药神 metalslime）**：

> 原文：「当下最重要的是远离未来加起来数万亿减持压力的板块，其他都是次要的；SOX的forward PE比日常消费品低，真流动性抽离时一波下跌让这些跟白酒一桌儿也不是不可能；白酒未来下跌动力不来自估值压缩（估值其实到底了），来自业绩下行负alpha驱动」

**单帖一条 + 优先级判定** → 整段归 **`predict`**（自上而下：无买卖动作/仓位 → 命中第 2 优先级「白酒未来下跌」满足三要素）→ 落库：

| content_type | target | stance | 提炼内容 |
|:---|:---|:---|:---|
| `predict` | 白酒 | **bearish** | 白酒未来下跌动力来自业绩下行负α（估值已到底，非估值压缩驱动）；当下应远离数万亿减持压力板块，SOX 估值虽低但流动性抽离时同样会跌 |

> **三重易错**：① 不得因「SOX 估值低」「白酒估值到底」而判 `neutral`（判据 ③ 论据不反转结论）；② **不得拆成多条**（单帖一条）；③ 按优先级，白酒的**未来判断**（predict）> 板块的当下规避（view）→ 归 `predict` 而非 `view`。

**标的解析须还原代称**：寒王→寒武纪、赵姨→兆易创新、兆易→兆易创新；原文代称写入 `targetAlias` 留痕，能挂上主题时带 `subjectId`。

**观点时间（双时间）判定**：默认 `view_date = statement_date`（`as_posted`）；原文写明日期 → `explicit`；含"三年前/今年五月份我的预判"等相对表述 → 以 `statement_date` 折算，置 `derived` 并把原句抄进 `view_date_basis`。粒度按表述给（只说"三年前"不得写成精确到日）。硬约束 `view_date ≤ statement_date`；跨度 ≥2 年或表述模糊（"很早以前"）→ 同时标待复核。转述他人判断不得算博主本人观点时间。

**能成 wiki 的一刀切判据**：内容是否提供**可脱离发帖语境复用的判断逻辑／框架／数据关系**？是 → wiki；否 → 只留言论（不硬造条目）。

**铁律（2026-09-08 画像单轨化 + 言论分表）**：言论/买卖/画像一律只落 MySQL（`blogger_statement`/`blogger_trade`/`update_blogger`；言论按类型物理分表 statement_research/statement_predict/statement_view/statement_insight/statement_chat/statement_trade，工具自动路由，见 framework-rules #39）。画像 md 已退役——不再镜像回写，vault 内残留画像文件仅为待删除的比对副本。**手改画像 md 表格、为画像文件写内容均为违规**。
**两条补充（2026-09-03）**：① 涉个股/行业/市场的言论调用 `blogger_statement` 时**必须传 `subjectId`**，否则该言论不会出现在「言论追踪」控制台——行业维度主题**按需创建**（词汇权威源 = tag-taxonomy 第五节申万分级；选最精确标准名，主题不存在则按标准名即时创建，**禁止自创非标准行业名**，见 framework-rules #38 行业主题按需创建）；② **写入前先查重**——同一段文字已存在于该博主的 `statements` 行时，合并/更新那一行，**禁止新增第二份**（"言论登记在谁名下就是谁的言论"既是归因依据，也是去重依据）。

### 分类不明处置（禁止静默丢弃 · 2026-09-06 用户规则）

分流时遇到 **①不属于任何既有 content_type**、或 **②定位模糊**（多类型皆可 / 明显有价值但去向不明）的言论：

1. **禁止丢弃、禁止强行套类**——强行套类会污染准确率追踪与子表结构，静默丢弃会永久丢失高价值言论，两者都比「列出来问一句」代价高。
2. **必须在执行汇报中单列「⚠ 待确认分类」一节**，逐条给出：原文全文（不得截断改写）、不确定原因（如「既有研究成分又有交易动作」「既非当下判断也无未来指向但明显是方法论雏形」）、候选建议（最接近的 1-2 个既有类型 + 理由，或「建议新增分类」+ 建议类名中英文名，供 dict 录入）。
3. **用户裁决两种结局**：**指定归属** → 按指定 content_type 正常经 `blogger_statement` 落库；**新增分类** → 走 dict 字典流程（`INSERT INTO dict`，type=`post_content_type`，remark 存判据、sort_order 定序，framework-rules #30 码值权威源——**只改字典，服务端校验与前端标签/tab 自动生效**，禁止在代码里加并行定义），再落库。
4. **裁决前**：该言论不写入任何表、也**不丢弃**，保持在待确认清单中（宁挂起、不流失）。
5. 「能成 wiki 的一刀切判据」仍然适用：分类不明 ≠ 没价值；确定有价值只是去向不明的，必须走本流程而不是丢弃。
